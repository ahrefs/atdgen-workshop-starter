{
  "name": "atdgen-workshop-starter",
  "version": "0.1.0",
  "scripts": {
    "format": "find . -iname '*.re' | grep -v node_modules | xargs bsrefmt --in-place && find . -iname '*.rei' | grep -v node_modules  | xargs bsrefmt -i true --in-place",
    "build": "bsb -make-world",
    "start": "bsb -make-world -w -ws _ ",
    "clean": "bsb -clean-world",
    "server": "bsb -make-world && node ./src_server/Server.bs.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "BuckleScript"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@glennsl/bs-json": "^5.0.4",
    "bs-express": "^1.0.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "reason-react": ">=0.9.1"
  },
  "devDependencies": {
    "bs-platform": "^9.0.2"
  }
}
