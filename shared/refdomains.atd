type datetime = string wrap <ocaml module="Wrap.Datetime">

type refdomain = {
  refdomain: string;
  backlinks: int;
  refpages: int;
  first_seen: datetime;
  last_visited: datetime;
  domain_rating: int wrap <ocaml module="Wrap.DomainRating">;
}

type response = {
  refdomains: refdomain list;
}
